import { Waiting } from '../Waiting/Waiting.js';

export const SwitchWalletsStep = () => {
  return <Waiting heading="Switching Wallets" subheading="Please complete the wallet selection in the popup window." />;
};
