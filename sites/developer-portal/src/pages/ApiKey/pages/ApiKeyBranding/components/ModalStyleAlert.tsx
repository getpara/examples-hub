import { InfoAlert } from '../../../../../components/InfoAlert';

export const ModalStyleAlert = () => {
  return (
    <InfoAlert
      title="Styling the Modal"
      content=" Branding and styling of your Para Modal is configured in your code using the Para SDK. See our styling guide."
    />
  );
};
