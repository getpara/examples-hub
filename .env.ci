# CI Environment Variables
# This file contains placeholder environment variables used for CI builds only.
# These are NOT used for E2E tests - those use real API keys from GitHub secrets.

# Core Para SDK variables (required for all builds)
PARA_API_KEY=test-build-key
VITE_PARA_API_KEY=test-build-key
NEXT_PUBLIC_PARA_API_KEY=test-build-key

# Para environment setting (optional - defaults to BETA if not set)
VITE_PARA_ENVIRONMENT=BETA
NEXT_PUBLIC_PARA_ENVIRONMENT=BETA

# Web3/Blockchain placeholders (only used by specific examples)
VITE_WALLET_CONNECT_PROJECT_ID=test-wc-id
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=test-wc-id

# Node.js memory settings
NODE_OPTIONS="--max-old-space-size=4096"