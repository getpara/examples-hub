# Security Note: Consider enabling script sandboxing for supply chain attack protection
# Yarn Berry (v2+) supports 'enableScripts: false' to disable postinstall scripts globally
# This would require migrating from Yarn Classic (1.x) to Yarn Berry
# Current protection: All CI workflows use --frozen-lockfile to prevent unauthorized package changes
yarnPath: .yarn/releases/yarn-1.22.22.cjs
