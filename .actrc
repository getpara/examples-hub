# act configuration to match GitHub Actions runner
# Runner version: 2.325.0
# OS: Ubuntu 24.04.2 LTS

# Use Ubuntu 24.04 image that matches GitHub's runner
-P ubuntu-latest=catthehacker/ubuntu:act-24.04

# Architecture
--container-architecture linux/amd64

# Reuse containers for faster runs
--reuse

# Use local .env file for secrets
--secret-file .env

# Secrets for E2E tests
# Replace these placeholder values with your actual API keys
-s PARA_API_KEY_BETA=your-beta-api-key-here
-s PARA_API_KEY_SANDBOX=your-sandbox-api-key-here

# Match GitHub environment
--env RUNNER_OS=Linux
--env RUNNER_ARCH=X64
--env ImageOS=ubuntu24
--env CI=true
--env GITHUB_ACTIONS=true

# Default event type
--eventpath .github/workflows/event.json